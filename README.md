# Telegram Bot with GitHub Integration

This repository contains a Telegram bot that integrates with GitHub. The bot uses the [Telegraf](https://telegraf.js.org/) library to interact with the Telegram API and provides basic functionality for GitHub integration.

## Project Structure
## Configuration

Create a `.env` file in the root directory with the following content:
Replace the placeholders with your actual values.

## Running the Bot

To run the bot locally, use the following commands:

```sh
npm install
npm start
### Commit and Push Changes

After updating the bot's functionality and the `README.md` file, commit and push your changes to GitHub:

```sh
git add .
git commit -m "Add /commits command to fetch latest GitHub commits"
git push origin main